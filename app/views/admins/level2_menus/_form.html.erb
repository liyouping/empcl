<%= form_for(admins_level2_menu) do |f| %>
  <% if admins_level2_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_level2_menu.errors.count, "error") %> prohibited this admins_level2_menu from being saved:</h2>

      <ul>
      <% admins_level2_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :link %>
    <%= f.text_field :link %>
  </div>

  <div class="field">
    <%= f.label :deleted_at %>
    <%= f.datetime_select :deleted_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
